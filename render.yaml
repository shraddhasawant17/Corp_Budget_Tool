services:
  - type: web
    name: rbl-budget-portal
    env: python
    buildCommand: pip install -r requirements.txt && python seed.py
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: freegi
